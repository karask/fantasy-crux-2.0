---
type: rule
id: start-here.dice-and-measures
chapter: start-here
title: Dice and Measures
slug: dice-and-measures
order: 20
summary: D100 uses a tens die and a units die, D6 means one six-sided die, and rules state how fractions are rounded.
aliases:
  - dice notation
  - rounding
  - metres
quickReference:
  group: core
  order: 20
---

## Dice notation

`D4`, `D6`, `D8`, `D10`, and `D12` name a die by its number of sides. A number before the `D` is how many to roll: `2D6` means roll two six-sided dice and add them. A flat modifier follows the dice, such as `1D6+2`.

For `D100`, roll two ten-sided dice: one gives tens and one gives units. Read `00` and `0` as 100; otherwise a zero on the tens die is a single-digit result. A tens die of `5` and units die of `9` is 59.

## Bonus and Penalty dice

`+1B` means one Bonus die; `-1P` means one Penalty die. These replace the tens die of a D100 test, not the skill value. See [Bonus and Penalty Dice](/rules/skills/bonus-and-penalty-dice/) for the full procedure.

## Measures and rounding

Distances are in metres and durations use ordinary seconds, minutes, and hours. On a grid, the group may treat each square as 1 or 2 metres, but must choose one scale for the scene.

Use the rounding method stated by the rule or formula. If none is stated, round to the nearest whole number; round exact halves up. Minimum damage after all reductions is 0.
